# MEMORY.md - 2026-02-24

- **2026-02-24 01:22 UTC 시스템 재정비:**
  - 사용자 요청에 따라 “SMTP 대신 Gmail로 직접 첨부 발송”으로 전달 방식 재설계 확정.
  - GitHub Action은 빌드/검증/아티팩트까지만 수행하도록 유지(빌드 오케스트레이션 역할은 로컬 루프).
  - 로컬 오케스트레이터 `scripts/ci_build_and_deliver.py`를 `gog gmail send --attach` 경로로 교체.
  - 핵심 변경 커밋: `c014f54` (푸시 완료).

- **현재 진행 상태:**
  - `run 52`는 최근 실행 중(`in_progress`)였고 `Build Debug APK` 단계 진행 확인됨.
  - `scripts/check_delivery_env.py`는 더 이상 SMTP가 아닌 gog 인증 상태 검사(`gog auth list --check`, `GOG_ACCOUNT`, `GOG_KEYRING_PASSWORD`)로 변경됨.

- **남은 이슈:**
  - `gog auth` 토큰 키링 복호화 오류 발생: `aes.KeyUnwrap(): integrity check failed`
  - 원인: `GOG_KEYRING_PASSWORD` 불일치/키링 손상 가능성. `GOG_KEYRING_PASSWORD` 재설정 또는 gog 계정 재인증 필요.

- **운영 약속:**
  - 빌드 성공 후 `ensure_smoke_pass` + 아티팩트 검증 통과 시,
    `kingjjy.game@gmail.com`로 APK 첨부 메일을 보내는 것을 최종 목표로 함.
